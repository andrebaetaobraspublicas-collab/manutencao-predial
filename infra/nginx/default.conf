upstream gestao_web {
  server web:3000;
  keepalive 32;
}

upstream gestao_api {
  server api:3001;
  keepalive 32;
}

server {
  listen 80;
  server_name gestaodepredios.com.br;
  return 301 https://www.gestaodepredios.com.br$request_uri;
}

server {
  listen 80;
  server_name www.gestaodepredios.com.br;

  location / {
    proxy_pass http://gestao_web;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
  }
}

server {
  listen 80;
  server_name api.gestaodepredios.com.br;
  client_max_body_size 20m;

  location / {
    proxy_pass http://gestao_api;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 120s;
  }
}
